# Optional: Override API URL (default: http://localhost:5001/api in dev)
# VITE_API_URL=http://localhost:5001/api

# Optional: Stream API key - if not set, fetched from backend /chat/token
# VITE_STREAM_API_KEY=your_stream_api_key